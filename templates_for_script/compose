services:
  hysteria:
    image: ${HYSTERIA_IMAGE}
    container_name: hysteria
    restart: always
    network_mode: host
    volumes:
      - ./hysteria/config.yaml:/etc/hysteria/config.yaml:ro
      - type: bind
        source: ${HYSTERIA_CERT_DIR}
        target: /etc/hysteria/tls
        read_only: true
    command: ["server", "-c", "/etc/hysteria/config.yaml"]
