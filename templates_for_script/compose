services:
  hysteria:
    image: tobyxdd/hysteria
    container_name: hysteria
    restart: always
    network_mode: "host"
    volumes:
      - acme:/acme
      - ./hysteria.yaml:/etc/hysteria.yaml
      - /opt/hysteria-vps-setup/templates:/opt/hysteria-vps-setup/templates
    command: ["server", "-c", "/etc/hysteria.yaml"]

volumes:
  acme:
